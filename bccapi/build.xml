<?xml version="1.0"?>
<project name="bccapi2" default="all" basedir=".">
	<property name="src.dir" location="${basedir}/src" />
	<property name="build.dir" location="${basedir}/out" />
	<property name="lib.dir" location="${basedir}/lib" />
	<property name="jar.dir" location="${build.dir}" />
	<property name="classes.dir" location="${build.dir}/classes" />
	<property name="doc.dir" location="${build.dir}/doc" />
	<property name="bitlib-jar.dir" location="../bitlib/out/" />

	<path id="common.classpath">
		<pathelement location="${bitlib-jar.dir}/bitlib.jar" />
	</path>

	<target name="bccapi2-jar" depends="bccapi2">
		<mkdir dir="${jar.dir}" />
		<jar destfile="${jar.dir}/bccapi2.jar" basedir="${classes.dir}">
			<fileset dir="${classes.dir}">
				<include name="**/*.class" />
			</fileset>
			<fileset dir="${src.dir}" includes="**/*.java" />
			<zipfileset excludes="META-INF/**/*" src="${bitlib-jar.dir}/bitlib.jar" />
            <manifest>
               <attribute name="Main-Class" value="com.bccapi.ng.example.SimpleClient"/>
            </manifest>
		</jar>
	</target>

	<target name="bccapi2">
		<mkdir dir="${classes.dir}" />
		<javac srcdir="${src.dir}" destdir="${classes.dir}" classpathref="common.classpath" debug="on" fork="true" failonerror="true" optimize="true" includeantruntime="false" />
	</target>

	<target name="javadoc" depends="bccapi2">
		<mkdir dir="${doc.dir}" />
		<javadoc excludepackagenames="org.test.supercsv.mock" access="public" destdir="${doc.dir}" author="true" version="true" use="true" windowtitle="BCCAPI2">
			<fileset dir="${src.dir}" defaultexcludes="yes">
				<include name="**/*.java" />
			</fileset>
		</javadoc>
	</target>

	<target name="all" depends="bccapi2-jar" />

	<target name="clean">
		<delete dir="${build.dir}" />
	</target>

</project>
